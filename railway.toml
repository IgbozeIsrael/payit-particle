[build]
  builder = "DOCKERFILE"
  dockerfilePath = "payit-particle/Dockerfile"
  dockerfileContext = "payit-particle"
  watchPaths = ["payit-particle/**"]

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
