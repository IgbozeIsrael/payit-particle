[build]
  builder = "DOCKERFILE"
  dockerfilePath = "payit-particle/Dockerfile"
  watchPaths = ["payit-particle/**"]

[deploy]
  workdir = "payit-particle"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10
